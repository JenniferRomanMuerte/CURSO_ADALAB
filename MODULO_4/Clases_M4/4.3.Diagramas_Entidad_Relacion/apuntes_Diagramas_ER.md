- Cuando asignamos el tipo de dato en la BBDD, a los campos que aunque sean númericos pensemos que no se van a hacer operaciones con ellos se guardan con VARCHAR
- Podemos declarar un tipo de campo que es ENUM, en este tipo de campo podemos definir varios valores fijos, por ejemplo
sexo ENUM(masculino, femenino)